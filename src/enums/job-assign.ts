export enum JOB_NAMES {
    CASHIER = 'Cashier',
    JANITOR = 'Janitor',
}

export enum SALARY {
    CASHIER = 450,
    JANITOR = 750,
}

export enum TIME {
    CASHIER = 40,
    JANITOR = 30,
}

export enum REQ {
    CASHIER = 1,
    JANITOR = 2,
}
